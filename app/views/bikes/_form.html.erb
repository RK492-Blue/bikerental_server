<%= form_with(model: bike, local: true) do |form| %>
  <% if bike.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bike.errors.count, "error") %> prohibited this bike from being saved:</h2>

      <ul>
      <% bike.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bike_serial_num %>
    <%= form.text_field :bike_serial_num, id: :bike_bike_serial_num %>
  </div>


  <div class="field">
    <%= form.label :bikestand_id %>
    <%= form.number_field :bikestand_id, id: :bike_bikestand_id %>

  </div>

  <div class="field">
    <%= form.label :available %>
    <%= form.text_field :available, id: :bike_available %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
